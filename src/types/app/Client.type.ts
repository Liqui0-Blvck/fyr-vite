export interface Client {
  id?: string; 
  name: string; 
  phoneNumber: string; 
  email: string; 
  status?: string; 
  createdAt?: string; 
  updatedAt?: string;
  notes?: string; 
  source?: string;
}
